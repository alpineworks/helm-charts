apiVersion: v1
kind: Service
metadata:
  name: {{ include "flux-suspensions-exporter.fullname" . }}
  labels:
    {{- include "flux-suspensions-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flux-suspensions-exporter.selectorLabels" . | nindent 4 }}
